@model SocialMediaApp.Models.Message


<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm">
                <div class="card-header bg-white py-3">
                    <h4 class="mb-0">
                        <i class="bi bi-chat-square-text me-2"></i>Editare mesaj
                    </h4>
                </div>

                <div class="card-body p-4">
                    <form asp-action="Edit" asp-controller="Messages" asp-route-id="@Model.Id" method="post">
                        <div class="form-group mb-4">
                            <label asp-for="Content" class="form-label">
                                <i class="bi bi-pencil me-2"></i>Conținut mesaj
                            </label>
                            <textarea asp-for="Content" class="form-control" rows="4"></textarea>
                            <span asp-validation-for="Content" class="text-danger small"></span>
                        </div>

                        <div class="d-flex justify-content-between">
                            <button class="btn btn-outline-success btn-sm" type="submit">
                                <i class="bi bi-check2 me-2"></i>Salvează modificările
                            </button>
                        
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border: none;
    }

    .form-control {
        border-radius: 8px;
        padding: 0.6rem 1rem;
    }

        .form-control:focus {
            border-color: #198754;
            box-shadow: 0 0 0 0.25rem rgba(25, 135, 84, 0.25);
        }

    .btn-sm {
        padding: 0.4rem 1.2rem;
        border-radius: 20px;
        font-size: 0.875rem;
    }

    .form-label {
        font-weight: 500;
        color: #495057;
        margin-bottom: 0.5rem;
    }
</style>